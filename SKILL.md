---
name: ai-novel-workflow
description: AI 长篇小说写作工作流。当出现「搭小说工作流」「建小说项目目录」「按这套工作流写第 N 章」「章节合同」「读者冷读」「回写数据库」「滚动细纲」「滚动复盘」等需求时应使用本 skill。覆盖新项目脚手架搭建与逐章八步循环（LOAD→CHECK→CONTRACT→DRAFT→REVIEW→REVISE→UPDATE→DECIDE）。核心原则：事实唯一（正文是唯一事实源）、只搭流程不代写正文、方向未经作者确认不得自动开写。
agent_created: true
---

# AI 长篇小说写作工作流

## Overview

「AI 执行 + 作者裁决」的长篇写作流程：AI 负责读资料、查冲突、写初稿、冷读、回写数据；作者负责定位、设定与每章最终裁决。

三条不可动摇的原则：

1. **事实唯一**——`正文/` 是唯一事实源；`待确认/` 稿件在作者裁决通过前不算事实，转正后从待确认目录删除
2. **先读后写**——每章动笔前读取本章所需资料，缺失就停下问作者，不编造
3. **人做判断**——AI 的产出（初稿、大纲、细纲）一律是草案，作者确认后才转正

## 模式 A：搭建新项目

1. 确认保存路径（用户未指定则先问）。
2. 盘点目标目录：已有小说正式文件一律保留，**不得覆盖**；只补齐缺失部分。
3. 用脚手架脚本复制模板并初始化 git（脚本确定性执行，不手写复制命令）：

   ```bash
   python3 scripts/init_novel_project.py "<项目目录>"
   ```

   已存在的文件默认跳过并列出。加 `--force` 可覆盖（慎用），`--no-git` 跳过 git。
4. 向作者交付四项报告，缺一不可：
   - 实际创建/补齐的文件清单
   - 与已有文件的冲突情况
   - 作者必须填写的内容（见 `references/template-guide.md` 的填写顺序）
   - 以后每章的执行顺序（八步循环）

搭建完成后**停下**，不写任何正文。

## 模式 B：逐章写作

用户口令「按这套工作流写第 N 章」时执行八步循环，顺序不可颠倒：

```
01 LOAD 读资料 → 02 CHECK 查冲突 → 03 CONTRACT 填合同 → 04 DRAFT 写初稿
→ 05 REVIEW 冷读 → 06 REVISE 修改 → 07 UPDATE 回写 → 08 DECIDE 作者裁决
```

- 初稿写进 `待确认/`，**不进 `正文/`**
- 冷读发现 S1（设定/逻辑）或 S2（节奏/追读）未清零 → 留在待确认
- DECIDE 通过后：移入 `正文/`、删除待确认副本、git 提交一次

**详细规程**（十问合同、冷读六问、回写六项、八条铁律、S1/S2/S3）见 `references/eight-step-loop.md`——写章节前读取该文件。

## 模式 C：滚动维护

- **滚动细纲**：只保持覆盖未来 5—12 章；每章确认后补新条目，跨档时新建文件（旧文件保留）
- **滚动复盘**：每 5—10 章提醒作者一次，三问——保留什么 / 停止什么 / 试验什么
- **设定变更**：正文与 02 冲突以正文为准，变更记入 02 登记表；被否决的设定进 00「禁止恢复的旧设定」
- **git 节奏**：每章裁决通过提交（正文+回写同批），方向调整单独提交

## 常见场景速查

| 场景 | 动作 |
|---|---|
| 作者资料填到一半 | 做资料自洽检查（清单见 `references/template-guide.md`），缺什么问什么 |
| 大纲/细纲要起草 | 起草后标注「草案 v1 · 待作者确认」，登记到 00 待确认项，留 3—4 个方向决策点给作者 |
| 作者想直接看效果 | 写试验稿进 `待确认/`，明确声明「不算正式事实」，转正需裁决 |
| 开书前想看市场 | 按 `references/platform-notes.md` 采集公开榜单，产出含落地启示的报告 |

## 资源

- `scripts/init_novel_project.py`：项目脚手架（复制模板 + git 初始化，不覆盖已有文件）
- `references/eight-step-loop.md`：逐章八步详细规程（写章节前读）
- `references/template-guide.md`：模板用途、填写顺序、资料自洽检查清单
- `references/platform-notes.md`：平台差异、赛道信号、市场调研方法
- `assets/novel-project-templates/`：19 个模板文件，由脚本复制，不手工逐个读
